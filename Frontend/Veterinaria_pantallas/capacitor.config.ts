import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Veterinaria_pantallas',
  webDir: 'build'
};

export default config;
